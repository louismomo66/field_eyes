# JWT Configuration
JWT_SECRET=fieldeystuliSmartbalimi
JWT_SECRETE=fieldeystuliSmartbalimi

# SMTP Configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=fieldeyes0@gmail.com
SMTP_PASSWORD=egby mdjt ultj dkwh
SMTP_FROM=fieldeyes0@gmail.com

# Redis Configuration (using IPv4 to avoid IPv6 issues)
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
# REDIS_PASSWORD=
REDIS_DB=0

# Database Configuration (Docker container)
DB_HOST=127.0.0.1
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres123456
DB_NAME=field_eyes

# Database URL (for Docker container)
DATABASE_URL=postgres://postgres:postgres123456@127.0.0.1:5432/field_eyes?sslmode=disable

# MQTT Configuration (using IPv4 to avoid IPv6 issues)
MQTT_BROKER_URL=tcp://127.0.0.1:1883
MQTT_USERNAME=
MQTT_PASSWORD=
MQTT_CLIENT_ID=field_eyes_server
MQTT_TOPIC_ROOT=field_eyes/devices

# Development mode (set to false to require database)
DEV_MODE=false

# Cloud deployment settings
# Uncomment these for Render or other cloud platforms
# PORT=10000
# DEV_MODE=true  # Use this to make the app start even without DB connections
# DB_HOST=your-postgres-host.render.com
# REDIS_HOST=your-redis-host.render.com 